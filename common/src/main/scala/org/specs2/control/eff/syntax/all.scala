package org.specs2.control.eff.syntax

object all extends all

trait all extends
  console with
  warnings with
  eval with
  option with
  disjunction with
  error with
  reader with
  writer with
  list with
  state with
  eff

