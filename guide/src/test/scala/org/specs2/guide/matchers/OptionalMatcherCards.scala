package org.specs2
package guide
package matchers

import form._

object OptionalMatcherCards extends Cards {
  def title = "Optional Matchers"
  def cards = Seq(
    XmlMatchers,
    JsonMatchers,
    FileMatchers,
    ContentMatchers,
    TerminationMatchers,
    ParserMatchers,
    InterpreterMatchers,
    ResultMatchers,
    DependencyMatchers)
}
