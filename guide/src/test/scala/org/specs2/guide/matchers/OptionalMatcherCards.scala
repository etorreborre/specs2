package org.specs2
package guide
package matchers

import form._

object OptionalMatcherCards extends Cards {
  def title = "Optional Matchers"
  def cards = Seq(
    ResultMatchers,
    XmlMatchers,
    JsonMatchers,
    FileMatchers,
    ContentMatchers,
    InterpreterMatchers,
    ParserMatchers,
    TerminationMatchers,
    DependencyMatchers)
}
